<div class="container">
  <h1><%= "#{@nudie.user.first_name} #{@nudie.user.last_name}" %> </h1>
  <ul class="list-unstyled">
    <%= render 'shared/nudie_profile' %>
    <% render 'shared/user_profile' %>
  </ul>
</div>
